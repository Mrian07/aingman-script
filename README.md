# v

```
apt update && apt upgrade -y && update-grub && sleep 2 && reboot
```

```
apt install -y && apt update -y && apt upgrade -y && wget -q https://raw.githubusercontent.com/Mrian07/aingman-script/main/setup.sh && chmod +x setup.sh && ./setup.sh
```
